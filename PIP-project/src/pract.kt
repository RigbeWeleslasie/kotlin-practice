import  kotlin.math.*
import java.util.Locale

fun main(){

    var arrays = arrayOf(1,2,3,4,5,6)
    var new = arrays.plus(5)
    println(new.contentToString())

    var strings= arrayOf("Rigbe", "Helen")
    var added= strings.plus("K")
    println(added.contentToString())
}
